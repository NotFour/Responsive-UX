/*
var progress = document.getElementById('progress');

progress = progress.getAttribute(value);
function changeProgress() {
    if (progress >= 90) {
        return;
    }
    progress++;
    setTimeout(changeProgress, 5);
}
changeProgress();*/
